# cube.c
it's a cool little program I wrote which was inspired by Andy Sloane's donut.c that displays a rotating cube in your terminal!

To compile it, run ```make```, and to install it, run ```sudo make install```.
